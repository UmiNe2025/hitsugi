# MISSION M28: 戦闘UX/バランス/回復薬/鍛冶master-detail — STATE

作成: 2026-07-17。/mission 起動(ユーザー5時間離席・自走)。前ミッション(M25/M26)は達成・公開済み。

## ①契約
**ゴール**: ユーザー明示の6要求 + 自走での追加改善。**バランス変更・新メカニクス(回復薬)は今回ユーザーが明示要求したためスコープ解禁**(従来全セッションで禁止だった領域)。

### 完了の定義(機械検証可能)
- **A. オート任意停止**: オート中でも任意のタイミングで停止できる。停止手段=オートボタン常時到達可能+手動コマンド入力で自動解除+Escape。**実測(playwright)**: オート開始→停止操作→次の入力番で自動攻撃が発火しない。
- **B. ゲームバランス(序盤の手応え)**: tier1で「1-2撃勝ち・被弾1固定」を是正。(1)ダメージ式に生ダメの下限割合を導入し防御での完全無効化を止める。(2)tier1敵の脅威度を調整。**戦闘シミュレーションharness(headless・既存battle関数)で前後の平均ターン数/被HP割合/敗北率を実測**(devil規律=推測でなく数値)。宿命モード(narrative=false)で有意に手応え増。**save互換維持**・ボス/高tierを破綻させない。
- **C. 回復薬(消耗品)**: `Item` に consumable 表現(optional effectフィールド)+回復薬データ数種(HP/灯/MP)+戦闘「道具」コマンド+鍛冶「見世」で購入。`BattleAction` に `'item'` 分岐(既存heal処理を再利用)。**save互換**(inventory は既存Item[]・optional拡張)。**実測**: 戦闘で道具→HP回復、購入で在庫増。
- **D. 視覚改善(3画面)**: ダンジョンHUD隊員HP札 / 戦闘コマンド盤 / 上部ターン順バー を「味気ない」→情報リッチで美しく。**実測**: 既存gateの矩形交差0・44px・可読を維持(退行なし)。
- **E. 鍛冶 master/detail(M26 §7)**: 一覧札=代表2軸(画像/名称/武防飾字印/主値/希少/装備中/主な差分1つ)、詳細=全軸。カード=選択、購入は詳細CTA。**実測**: 一覧クリックで詳細表示、閲覧で購入発生しない。
- **F. 自走の追加改善**: 監査で発見した高価値項目を可能な範囲で(各々に受入証跡)。
- **G. 統合**: `npm run build`緑 / `lint`0 / `vitest`緑(現533→増) / `validate_data`0 / `test:visual`緑 / `index.css`非編集 / **push無し(ユーザーゲート・帰還後判断)**。

### スコープ外
- **push(公開)** — ユーザーゲート。今回は明示承認が無いため公開しない。帰還後にレビュー+デプロイ判断。
- 老い姿/幼子スプライト144枚の正規再生成(ComfyUI工場・別途)。
- M26 Phase 1-5 の残り(ホーム/普請/星契り大改修/家譜/家系図/設定 — E以外)。

### エスカレーション条件
push が必要 / 契約・スコープ変更 / save非互換が不可避と判明 / 同一項目2連敗。

### 監査区分(単調ラッチ)
**自己監査** — 完了定義に不可逆成果物・出荷を含まない(push無し・全変更git可逆)。補強: **devil攻撃1回**(バランス式変更/回復薬経済/save互換の観点)+ **バランスはシミュレーション実測**で受入。

### 主観項目の受入(契約時固定)
視覚の美しさ(D)は「既存gateの数値退行なし+ユーザー実走」で受入し、主観部は⚠️許容。バランス(B)は数値(平均ターン/被HP/敗北率)で受入。

## ②作業分解
- A オート停止 — 本体Opus(戦闘フロー)。
- B バランス — 本体Opus(難所: 式設計+シミュレーションharness+実測)。
- C 回復薬 — 本体Opus(難所: 型/データ/performAction/UI/購入)。
- D 視覚3画面 — sonnet委譲(所有: dungeon HUD=Dungeon.tsx+dungeon_m25.css / 戦闘コマンド・ターン順=Battle.tsx+battle_m25.css)。※Battle.tsxはA/Cで本体も触るため直列化。
- E 鍛冶 master/detail — sonnet委譲(所有: Forge.tsx+forge_m26.css)。
- F 追加改善 — 監査後に割付。
- G 統合 — 本体。

## ③完了済み(証跡)
(未着手)

## ④保留リスト
(なし)

## ⑤質問キュー
(空 — ユーザー離席中。判断は sensible default で進め、可逆性を保ち、⑥へ記録)

## devil攻撃反映(2026-07-17・REWORK→PROCEED WITH MITIGATIONS)
1. **C 回復薬は ItemSlot にしない**(セーブ破損地雷: 装備一覧漏れ→死亡時消滅/SLOT_ORDER NaN/家宝汚染/inheritItem誤混入)。→ **`data.consumables` 新設**(装備/鍛冶/打ち直し/形見/家宝/継承から不可視)。受入: 消耗品購入→装備一覧'all'で不在 / 保持者死亡でitem損失0 / 消耗品入りセーブ load→save→load 安定。
2. **B 攻防は直交**: 「1-2撃勝ち」=tier1敵hp/atk のみ、「被弾1固定」=ダメージ下限 のみ。無検証で重ねない。下限は**全戦闘(ボス含む)に発火** — 玄冬(atk85)vs 高防御(def~122)は現状1固定、下限0.15で約10へ(約10倍)。→ **ボス安全表(玄冬/汐里/tier4老 × narrative on/off)で参照ステータス party が勝てることを確認してから出荷**。tier-scoped下限を優先検討。
3. **B harness忠実性**: narrative易化(敵atk/hp×0.78)・morale・変異(若×0.62/老×1.55)を複製しないと架空戦闘。指標=**瀕死率(味方が<30%に落ちた戦闘割合)+run消耗**、平均ターン数単独は不可。代表party複数(素手gen1/装備gen3)。
4. **A 実装済**(本セッション `8998a39` で完了・auto_stop.spec緑)。再設計しない。
5. **D 追加ゾーン**: 既存[data-zone]矩形とcommand44px/横スクロール0(mobile-360)を保つ。`.shots`再生成で退行隠蔽しない。
6. **ストップロス**: build/lint/vitest/gate/auto_stopのいずれか赤で1回で緑にならなければ停止しWORKLOG記録。index.css非編集。

## ⑥マイルストーン履歴
- Phase0: ディスカバリー(バランス根因=減算防御で弱攻撃1固定/1-2撃勝ち・回復薬は空白領域)→契約→devil攻撃→発進(2026-07-17)。

## ⑦次の一手
オート停止(A・低リスク先行)→ シミュレーションharness構築 → バランス(B)→ 回復薬(C)。D/Eは並行委譲。

## ⑧最終監査表
(未実施)

## ⑨terminal印
(未達 — 実行中)
